apiVersion: v1
kind: Service
metadata:
  labels:
    app: ps-site-ui
  name: ps-site-ui
  namespace: tf
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: ps-site-ui
